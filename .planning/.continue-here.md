---
context: research
phase: null
task: firewall-live-validation
total_tasks: null
status: paused
last_updated: 2026-08-18T08:06:11.507Z
---

# Handoff — Defender, OpenCTI y demostración Firewall

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Estas restricciones se descubrieron mediante fallos reales. Deben comprobarse antes de continuar.

- [ ] **WSL no está instalado:** `wsl.exe --list --verbose` abrió la solicitud de instalación, exigió elevación y fue cancelada. No reinstalar ni volver a basar la demostración en WSL.
- [ ] **Los snapshots pueden retirar el Adaptador 2:** después de restaurar, comprobar nuevamente `vboxnet0`, `192.168.56.1` y la IP Host-only de Windows.
- [ ] **No existe integración Defender→OpenCTI:** no fabricar un `Sighting`, una alerta o una relación para insinuar que OpenCTI recibió los eventos 1116/1117.

**No continuar hasta comprobar las tres restricciones.**

## Critical Anti-Patterns

| Patrón | Descripción | Severidad | Prevención |
|---|---|---|---|
| Depender de WSL | La VM no lo tiene y el intento condujo a un instalador con elevación. | blocking | Usar el anfitrión Linux por VirtualBox Host-only. |
| Confundir modelado CTI con telemetría | Los objetos manuales de OpenCTI no prueban una publicación de Defender. | blocking | Eventos 1116/1117 en Windows como evidencia; OpenCTI solo como contexto. |
| Omitir el preflight tras un snapshot | Restaurar puede revertir hardware virtual y configuraciones posteriores. | blocking | Comprobar ambos adaptadores e IP antes de cada ensayo. |
| Reescribir en vez de ampliar | El material publicado es la espina dorsal de la clase. | blocking | Insertar o corregir bloques; no sustituir el contenido que el usuario ya dicta. |
| Probar con privilegios distintos | Un comando ensayado como administrador puede fallar para `operador_alumno`. | blocking | Ejecutar cada paso con la misma cuenta y elevación indicadas en el guion. |
| Editar EICAR a ojo | Se copió inicialmente un SHA-256 de 65 caracteres y surgió la tentación de borrar uno arbitrariamente. | advisory | Validar 68 bytes y el SHA-256 completo; nunca truncar hashes. |
| Escribir EICAR completo en el repositorio | Defender podría poner en cuarentena el material al clonar en Windows. | advisory | Mantener la cadena dividida y concatenarla solo durante la ejecución. |
| Asumir que el laboratorio quedó limpio | El servidor y una regla con nombre provisional se crearon manualmente. | advisory | Consultar reglas, puertos y procesos antes de repetir. |

<current_state>

El hito GSD v1.0 está completo; no existe una fase activa. El trabajo actual es investigación y pulido posterior del contenido de Asignatura 1 mediante demostraciones ejecutadas en la VM del instructor.

La rama `main` está limpia y sincronizada con `origin/main`. HEAD es `15f0d73 docs: demostrar reglas firewall inbound y outbound`.

Microsoft Defender detectó correctamente EICAR. El evento 1116, RecordId 545, registró `Virus:DOS/EICAR_Test_File`; el evento 1117, RecordId 548, registró la cuarentena. El archivo desapareció y `Get-MpThreat` lo mostró inactivo.

OpenCTI se conserva solo como contexto de inteligencia: System `VM-WIN-SI - Instructor`, File Observable de EICAR, Indicator basado en el hash y un Observed Data manual. No se creó un Sighting y no debe simularse una integración.

La prueba Firewall utiliza Adaptador 1 NAT y Adaptador 2 Host-only. El anfitrión Linux tiene `192.168.56.1`; Windows tiene `192.168.56.101` en Host-only y `10.0.2.15` en NAT. `Ethernet 2` mostró una APIPA `169.254.47.53` y no se usa.

La línea base outbound fue comprobada: desde Windows, `Test-NetConnection 192.168.56.1 -Port 18081` devolvió `True` e `Invoke-WebRequest` devolvió HTTP 200. Todavía falta ejecutar y registrar el bloqueo, la recuperación y toda la prueba inbound.

</current_state>

<completed_work>

- Demostración segura del ciclo de firmas con EICAR ejecutada y documentada en `.planning/research/DEFENDER-OPENCTI-SIGNATURE-DEMO.md`.
- OpenCTI retirado del camino crítico: no se presentará un Observed Data o Sighting manual como detección automática.
- Práctica 13 confirmada: detección, evento 1116, cuarentena, evento 1117 y archivo ausente.
- Práctica 14 ampliada con comprobación de elevación, protección en tiempo real y línea base de exclusiones. Commit `f98cc65`.
- Terminología visible normalizada de «Cortafuegos» a «Firewall» sin renombrar archivos internos.
- Demostración WSL sustituida por NAT + Host-only y explicada desde la perspectiva de Windows.
- Prueba outbound de línea base validada con conexión TCP y HTTP 200.
- Guion final con outbound e inbound, resultados esperados y limpieza publicado en `15f0d73`.
- `git diff --check`, sintaxis Bash y `python3 -m mkdocs build --strict` aprobados.

</completed_work>

<remaining_work>

1. Limpiar el estado manual del ensayo anterior en el anfitrión y Windows.
2. Ejecutar completa la Prueba 1 outbound publicada: línea base, Block, ausencia de `GET`, Disabled y recuperación HTTP 200.
3. Ejecutar completa la Prueba 2 inbound publicada: listener, Allow, mensaje recibido, Block, ausencia de mensaje, Disabled y recuperación.
4. Corregir `cortafuegos-p1.md` si las salidas reales difieren del guion y volver a validar MkDocs.
5. Ejecutar la Práctica 14 de exclusiones de Defender con consola elevada; confirmar que EICAR sobrevive solo dentro de la exclusión y que la limpieza la elimina.
6. Confirmar y enviar cualquier ajuste resultante.

</remaining_work>

<decisions_made>

- No instalar WSL: introduce elevación, dependencia y posible reinicio sin aportar valor didáctico.
- Mantener NAT para Internet y añadir Host-only para el tráfico de laboratorio.
- Interpretar direcciones desde Windows: Windows→Linux es outbound; Linux→Windows es inbound.
- Usar IP y puerto exactos en las reglas para no afectar el adaptador NAT ni otros servicios.
- Mostrar primero una línea base funcional; así el fallo posterior puede atribuirse a la regla.
- Mantener OpenCTI como explicación opcional de Observable/Indicator, no como dashboard de detección.
- Mantener los nombres de archivo `cortafuegos-p1.md` y `cortafuegos-p2.md` para no romper enlaces.

</decisions_made>

<blockers>

No hay un bloqueo técnico permanente. Hay dos estados externos que deben verificarse manualmente antes de repetir: una posible regla provisional en Windows y un posible servidor HTTP aún abierto en el anfitrión.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — estado estructurado y acciones pendientes.
2. `docs/seguridad-criptografia/en-el-ordenador/cortafuegos-p1.md`, desde «Demostración visible de Inbound y Outbound» — guion que debe ejecutarse.
3. `docs/seguridad-criptografia/en-el-ordenador/antivirus-avanzados-p1.md`, Prácticas 13 y 14 — estado de la demostración Defender.
4. `.planning/research/DEFENDER-OPENCTI-SIGNATURE-DEMO.md` — evidencia EICAR y límites correctos de OpenCTI.
5. `.planning/STATE.md` — contexto del curso y restricciones históricas.

## Critical Anti-Patterns (do NOT repeat these)

- No instalar WSL para salvar el guion: revisar primero la imagen real y adaptar la práctica.
- No crear `related-to`, `Sighting` o alerta manual para fingir una integración Defender→OpenCTI.
- No borrar un carácter arbitrario de un hash: recalcular y validar longitud.
- No restaurar un snapshot y asumir que Adapter 2 sigue conectado.
- No ejecutar comandos de alumno como administrador durante la validación.
- No escribir la cadena EICAR completa de forma contigua en archivos versionados.

## Infrastructure State

- **Repositorio:** `main` limpio, `HEAD=15f0d73`, sincronizado con `origin/main`.
- **VirtualBox:** Adaptador 1 NAT; Adaptador 2 Host-only `vboxnet0`.
- **Linux anfitrión:** `192.168.56.1/24`; puede quedar un `python3 -m http.server 18081` abierto fuera de Codex.
- **Windows VM:** Host-only `192.168.56.101`; NAT `10.0.2.15`; APIPA no usada `169.254.47.53`.
- **WSL:** no instalado; el usuario canceló correctamente el instalador.
- **Firewall Windows:** puede quedar la regla provisional `LAB - Bloquear salida TCP 18081 al anfitrion`; el guion final usa `LAB-FW-OUT-BLOCK-18081`.
- **Defender:** protección en tiempo real activa; EICAR fue puesto en cuarentena; eventos 1116/1117 confirmados.
- **OpenCTI:** desplegado en VPS y accedido mediante túnel SSH como `localhost:8080`; no hay conector Defender.
- **Procesos Codex:** ninguno relevante; el subagente de investigación está interrumpido.
- **Trabajos asíncronos:** ninguno.

## Pre-Execution Critique Required

- **Artefactos:** `cortafuegos-p1.md` y la Práctica 14 de `antivirus-avanzados-p1.md`.
- **Foco:** privilegios correctos, dirección del flujo, precedencia Block sobre Allow, visibilidad en ambos extremos, limpieza y supervivencia del Adaptador 2 tras snapshots.
- **Gate:** no dar por aprobadas la prueba inbound ni la Práctica 14 hasta ejecutarlas en la VM real y comparar cada salida con el texto.

<context>

El usuario está puliendo el contenido mediante estudio guiado y ejecución real. Prefiere avanzar paso a paso, entender qué demuestra cada comando y eliminar ambigüedades antes de aprobar el guion.

La prioridad inmediata ya no es OpenCTI. Es terminar la verificación real de las reglas Firewall y después ensayar la exclusión controlada de Defender.

</context>

<next_action>

Empezar en PowerShell Administrador consultando y retirando, si existe, la regla provisional `LAB - Bloquear salida TCP 18081 al anfitrion`. Detener cualquier servidor HTTP previo, comprobar `vboxnet0` y las IP, y ejecutar desde el Paso 1 la Prueba 1 outbound publicada.

</next_action>
