---
context: phase
phase: 02.1-correccion-iptables
task: 6
total_tasks: 6
status: in_progress
last_updated: 2026-08-19T04:23:57-05:00
---

# Handoff — Fase 2.1: corrección labs iptables (solo queda la UAT)

<current_state>

Tareas 1-5 del plan `02.1-01-PLAN.md` completas, commiteadas y **pushed** en
`a553a6b` (main = origin/main). Las cuatro lecciones de iptables ya están
reescritas para VM Ubuntu 24.04 y publicadas; `mkdocs build --strict` verde;
los 13 hallazgos de la auditoría cerrados (tabla en `02.1-01-SUMMARY.md`).

Solo queda la **Tarea 6: UAT real en la VM**, bloqueada porque la VM Ubuntu
24.04 aún no existe y el disco del anfitrión está al 98 %.

</current_state>

<completed_work>

- Tarea 1: Fase 2.1 en ROADMAP.md (edición manual, estilo raya) — Done (local)
- Tarea 2: `lab-iptables-p1.md` reescrita — Done (`a553a6b`)
- Tarea 3: `lab-iptables-p2.md` reescrita — Done (`a553a6b`)
- Tarea 4: soluciones instructor alineadas — Done (`a553a6b`)
- Tarea 5: build estricto + cierre 13/13 hallazgos — Done (`a553a6b`)

</completed_work>

<remaining_work>

- Tarea 6: UAT en la VM — ejecutar P1 completa (incluida "Preparación previa"
  con snapshot `base-limpia`) y luego P2, como alumno, con el host Windows como
  segundo extremo por la red Host-only. Registrar toda desviación entre salidas
  reales y las documentadas; corregir material; actualizar SUMMARY a complete.

Verificaciones que la UAT debe cubrir explícitamente:
1. P1 Paso 10 — prueba DROP por orden: `-I INPUT 1` bloquea y el contador lo
   demuestra; variante `-A` queda inerte (demo instructor).
2. P2 Paso 9 — sonda al 8080 desde PowerShell produce línea `IPTABLES-DROP:`
   con DPT=8080 **y** contador de la regla LOG > 0.
3. P2 Paso 11 — tras `systemctl reboot`, el ruleset del escenario está activo
   sin intervención.
4. P2 Paso 12 — limpieza: restore inicial + segundo `netfilter-persistent save`
   (sin él, la política DROP revive al arrancar).
5. Salidas de Ubuntu 24.04 coinciden con lo documentado: `iptables v1.8.10
   (nf_tables)`, columna prot `0/6`, `ctstate`, comentarios `/* */`.

</remaining_work>

<decisions_made>

- Listener HTTP en puerto 80 con sudo; alternativa 8080 como nota.
- Persistencia real (netfilter-persistent + reboot); save/restore renombrado
  "backup manual". El contraste es el contenido didáctico central de P2.
- Comentarios de reglas con `-m comment` (sobreviven a save/restore).
- ROADMAP editado a mano (parser `phase.insert` incompatible con estilo raya).

</decisions_made>

<blockers>

- VM Ubuntu 24.04 inexistente (acción humana): crearla en VirtualBox con NAT +
  Host-only desde `isos/ubuntu-24.04.4-desktop-amd64.iso`.
- Disco anfitrión al 98 % (acción humana): retirar `ova/VM-WIN-SI.ova` (32 GB)
  antes de crear la VM y sus snapshots.

</blockers>

## Required Reading (in order)

1. `.planning/phases/02.1-correccion-iptables/02.1-01-SUMMARY.md` — qué se hizo
   y la tabla de cierre de hallazgos.
2. `.planning/phases/02.1-correccion-iptables/02.1-01-PLAN.md` — Tarea 6 (UAT)
   con sus verificaciones.
3. `docs/seguridad-criptografia/en-el-ordenador/lab-iptables-p1.md` y
   `lab-iptables-p2.md` — el material que la UAT ejecuta.
4. `.planning/research/IPTABLES-LESSON-AUDIT.md` — solo como referencia de qué
   se corrigió.

## Critical Anti-Patterns (do NOT repeat these)

- No usar `phase.insert` sobre este ROADMAP (headings con raya rompen el parser).
- No declarar la UAT "pasada" sin ejecutar los 5 puntos de verificación listados
  arriba en la VM real — la validación estática ya está hecha; repetirla no es UAT.
- No editar las lecciones por hallazgos nuevos sin registrar la desviación en el
  SUMMARY (el material publicado y el SUMMARY deben contarse la misma historia).

## Infrastructure State

- Sin procesos de fondo, watchers ni trabajos asíncronos.
- `main` = `origin/main` = `a553a6b`; GitHub Actions despliega el sitio solo.
- `.planning/` gitignored salvo STATE.md y HANDOFF.json (force-added).
- `isos/ubuntu-24.04.4-desktop-amd64.iso` disponible en local (ignorada por git).
- Directorio `.superpowers/` untracked — artefacto de plugin, ignorar.

<context>

La fase 2.1 está funcionalmente terminada del lado documental: material
corregido, publicado y auto-consistente. La UAT es deliberadamente lo único
pendiente porque requiere hardware/acciones del usuario (espacio en disco y
creación de la VM). Al resumir: si la VM ya existe, ir directo a la Tarea 6;
si no, las dos acciones humanas de <blockers> van primero.

</context>

<next_action>

Start with: confirmar si la VM Ubuntu 24.04 existe. Si sí → ejecutar Tarea 6
(UAT completa P1→P2). Si no → recordar al usuario liberar disco y crear la VM;
no hay trabajo documental pendiente mientras tanto.

</next_action>
