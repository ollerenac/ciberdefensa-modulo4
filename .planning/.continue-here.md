---
context: research
phase: null
task: 5
total_tasks: 6
status: paused
last_updated: 2026-08-20T11:27:12.973Z
---

# Handoff — examen parcial aleatorio en MoodleCloud

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Estas restricciones surgieron de fallos reales durante la implementación.
> Deben reconocerse antes de continuar.

- [ ] CONSTRAINT: frontera privada obligatoria — un token temporal y maquetas
  llegaron a quedar versionados bajo `.superpowers/`; ya fueron retirados en la
  rama del examen — verificar `.private/` y `.superpowers/` ignorados antes de
  crear o añadir cualquier artefacto del examen.
- [ ] CONSTRAINT: aislamiento de rama — `main` avanzó mientras se trabajaba en el
  examen — continuar exclusivamente en `.worktrees/examen-moodlecloud` sobre
  `feature/examen-moodlecloud`; no copiar cambios ni implementar sobre `main`.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Publicar secretos del examen | `.superpowers/.last-token` y maquetas quedaron rastreados antes de activar las exclusiones. | blocking | Trabajar bajo `.private/examen-parcial/`; verificar `git check-ignore`, `git status` y `git ls-files` antes de cada commit. |
| Reintroducir plan privado | El diseño y el plan del examen estuvieron bajo `docs/` y fueron retirados en `b0ed7d9`. | blocking | Leer sólo las copias ignoradas de `.planning/resume/moodlecloud/`; nunca restaurarlas en `docs/`. |
| Reusar el piloto sin saneamiento | El primer runbook podía consumir el único intento de una cuenta final. | advisory | Usar piloto independiente; si se usa una cuenta final, borrar el intento y verificar su ausencia antes de asignarla. |
| Desviarse del objetivo Moodle | Durante la sesión surgió una consulta no relacionada y el usuario pidió control explícito del alcance. | advisory | Señalar el desvío, posponerlo y volver a MoodleCloud/exámenes. |

<current_state>

Las Tareas 1–4 del plan MoodleCloud están implementadas y revisadas en el
worktree aislado:

- Ruta: `/home/researcher/Teaching/inictel/clase_al_ejercito_2026/.worktrees/examen-moodlecloud`
- Rama: `feature/examen-moodlecloud`
- HEAD: `f99fab7`
- Árbol Git: limpio
- Base de la rama: `511354c`
- `main`: `160355b`, sincronizada con `origin/main` y con cambios posteriores
  de laboratorios que no deben sobrescribirse.

La ejecución está pausada en el checkpoint humano previo a la Tarea 5. El banco
privado pasó validación estructural y revisión factual/calidad. Falta que el
instructor lea la vista ciega y apruebe el nivel pedagógico. También falta
confirmar un método real de aliases controlados antes de producir cuentas.

</current_state>

<completed_work>

- Tarea 1 — límites `.private/`/`.superpowers/`, validador GIFT y pruebas:
  completada en `7334ad6`; 10 pruebas verdes; artefactos temporales retirados
  del índice y preservados privadamente.
- Tarea 2 — banco privado de 20 preguntas y auditoría factual: completada sin
  commit por confidencialidad; matriz 6 CDE + 6 DEF + 6 FWA + 2 GEN; dos rondas
  de revisión cerradas; fuentes primarias vigentes.
- Tarea 3 — generador de cuentas: `d6dae34`, `9c33e79`, `cab18fd`; 30 pruebas
  verdes; rutas privadas, emails, contraseñas, permisos 0600 y escapes de ruta
  cubiertos; no se generaron CSV reales.
- Tarea 4 — página pública segura y runbook: `1c53854`, `71c552d`, `4c9b867`,
  `f99fab7`; build estricto, suite y escaneos de fuga verdes; runbook revisado en
  tres rondas.
- Vista ciega privada creada en
  `.private/examen-parcial/revision-instructor.md`: 20 IDs, 80 alternativas
  mezcladas y ninguna marca de respuesta.

</completed_work>

<remaining_work>

1. Checkpoint del instructor:
   - abrir únicamente `.private/examen-parcial/revision-instructor.md`;
   - responder `Banco aprobado` o listar IDs con observaciones;
   - no abrir la auditoría o el GIFT durante la lectura ciega.
2. Confirmar si existe un buzón controlado con aliases `+` o 20 emails únicos.
3. Generar privadamente `usuarios-numerados.csv` y `asignacion-alumnos.csv`;
   verificar 21 líneas y ausencia en `git status`.
4. Tarea 5 — crear/abrir MoodleCloud, curso oculto `SEGINFO-ORD`, importar GIFT,
   configurar 3+3+3+1 y ejecutar un piloto independiente.
5. Tarea 6 — contingencia A/B, cargar las 20 cuentas, cinco previsualizaciones,
   prueba 10/20, desconexión, exportación y cierre de seguridad.
6. Revisión integral de la rama, reconciliación con `main` y flujo de cierre.

</remaining_work>

<decisions_made>

- MoodleCloud será el servidor; no se desarrollará una aplicación propia.
- El intento contiene 10 preguntas aleatorias 3+3+3+1, una por página,
  navegación libre, un intento y nota máxima 20.
- Todo banco, respuesta, identidad, URL y evidencia vive bajo
  `.private/examen-parcial/` y nunca en Git/MkDocs/chat.
- La cuenta piloto recomendada es independiente de `alumno01`–`alumno20`.
- El usuario introduce personalmente credenciales administrativas y la
  contraseña temporal del quiz.
- El agente debe advertir y posponer cualquier tema ajeno a Moodle/exámenes.

</decisions_made>

<blockers>

- Aprobación humana: la vista ciega aún no tiene veredicto del instructor.
- Identidades: no se ha confirmado un patrón real de aliases o emails únicos.
- MoodleCloud: la configuración, importación y UAT requieren acciones manuales
  del usuario; el agente no dispone ni debe solicitar credenciales.
- Integración: la rama del examen quedó detrás de `main`; reconciliar sólo al
  cerrar la implementación, preservando los cambios posteriores de laboratorios.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — estado estructurado y acciones humanas.
2. `.superpowers/sdd/2026-08-19-moodlecloud-examen-parcial/progress.md` — ledger
   autoritativo de tareas, rondas de revisión, rulings y pendientes menores.
3. `.planning/resume/moodlecloud/2026-08-19-moodlecloud-examen-parcial-design.md`
   — especificación funcional privada.
4. `.planning/resume/moodlecloud/2026-08-19-moodlecloud-examen-parcial.md` — plan
   privado; continuar en Tarea 5.
5. `.private/examen-parcial/revision-instructor.md` — vista ciega que debe leer
   el usuario, no el agente para aprobar en su nombre.
6. `docs/instructor/examen-parcial-moodlecloud.md` — procedimiento operativo ya
   revisado.
7. Skill `superpowers:subagent-driven-development` — reanudar el ledger, no
   redispatchar Tareas 1–4.

## Critical Anti-Patterns (do NOT repeat these)

- No leer, copiar ni publicar preguntas/respuestas en el chat o en informes de
  agentes; reportar sólo IDs, conteos y naturaleza de hallazgos.
- No restaurar el diseño o plan en `docs/`; sus copias actuales son privadas.
- No generar cuentas con dominios inventados ni ejecutar el ejemplo
  `example.invalid`.
- No pedir ni almacenar contraseña administrativa, URL operativa, cookies o
  contraseña temporal del cuestionario.
- No trabajar en `main` ni sobrescribir sus cambios posteriores; usar el
  worktree existente.
- No volver a despachar Tareas 1–4: el ledger y los commits prueban su cierre.

## Infrastructure State

- Worktree activo: `.worktrees/examen-moodlecloud`.
- Rama/HEAD: `feature/examen-moodlecloud` / `f99fab7`.
- Rama principal: `main` / `160355b`, limpia y sincronizada con `origin/main`.
- Pruebas: 30/30 verdes en la última ejecución.
- MkDocs: `python3 -m mkdocs build --strict` verde tras Tarea 4.
- Banco: validador verde, 20 preguntas y categorías 6+6+6+2.
- Artefactos privados: ignorados; no aparecen en `git status`.
- MoodleCloud: sitio existente según el usuario; configuración del curso/quiz no
  ejecutada en esta sesión. No registrar URL ni identidad administrativa.
- Agentes, servidores, watchers y trabajos asíncronos: ninguno activo.

<context>

La implementación local está avanzada y estable. El siguiente trabajo no es
escribir más código: primero se necesita el juicio pedagógico del instructor y
un método legítimo para los emails. Después se genera el CSV privado y se guía la
configuración manual de MoodleCloud mediante checkpoints, sin manejar secretos.

El SDD sigue siendo el registro de ejecución. Las Tareas 1–4 ya tienen revisión
independiente; al reanudar, no repetirlas. Los hallazgos menores diferidos del
generador y del orden del runbook deben llegar a la revisión integral final.

</context>

<next_action>

Start with: entrar al worktree, leer el ledger SDD y pedir al usuario dos
respuestas: (1) `Banco aprobado` o los IDs con observaciones después de leer la
vista ciega; (2) si dispone de aliases `+` controlados o de 20 emails únicos. No
solicitar valores secretos. Con ambas respuestas, generar los CSV privados y
comenzar la Tarea 5 con acciones manuales en MoodleCloud.

</next_action>
